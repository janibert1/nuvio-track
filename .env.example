# Get one free at https://www.themoviedb.org/settings/api
TMDB_API_KEY=

# Reused from an existing project - google-genai SDK key
GOOGLE_API_KEY=

# Shared secret for the /history, /ratings, /watchlist, /recommendations
# endpoints. Generate with: python3 -c "import secrets; print(secrets.token_hex(24))"
NUVIOTRACK_API_KEY=

# Where the SQLite file lives
DB_PATH=./nuviotrack.db
